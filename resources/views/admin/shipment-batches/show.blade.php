@extends('layouts.app')
@section('content')
<div class="d-flex justify-content-between"><h1>{{ $shipmentBatch->name }}</h1><a class="btn btn-outline-secondary" href="{{ route('admin.shipment-batches.edit',$shipmentBatch) }}">Edit</a></div>
<div class="card mb-4"><div class="card-body"><p>Status: <strong>{{ $shipmentBatch->status }}</strong></p><p>Departure: {{ $shipmentBatch->departure_date?->format('d M Y') ?? 'Not set' }} | Arrival estimate: {{ $shipmentBatch->arrival_estimate?->format('d M Y') ?? 'Not set' }}</p></div></div>
<div class="card mb-4"><div class="card-body"><h3>Add paid order</h3><form method="POST" action="{{ route('admin.shipment-batches.orders.attach',$shipmentBatch) }}" class="row g-2">@csrf<div class="col-md-8"><select name="order_id" class="form-select">@foreach($availableOrders as $order)<option value="{{ $order->id }}">{{ $order->order_number }} - {{ $order->customer->name }} - {{ $order->currency }} {{ number_format($order->total_amount,2) }}</option>@endforeach</select></div><div class="col-md-4"><button class="btn btn-warning">Add to Batch</button></div></form></div></div>
<table class="table card card-body"><tr><th>Order</th><th>Customer</th><th>Status</th><th></th></tr>@foreach($shipmentBatch->orders as $order)<tr><td>{{ $order->order_number }}</td><td>{{ $order->customer->name }}</td><td>{{ $order->status->value }}</td><td><form method="POST" action="{{ route('admin.shipment-batches.orders.detach',[$shipmentBatch,$order]) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">Remove</button></form></td></tr>@endforeach</table>
@endsection
